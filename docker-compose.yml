# version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    # Uncomment to mount code for dev
    # volumes:
    #   - ./:/app